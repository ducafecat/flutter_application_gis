import 'package:flutter/material.dart';

class CompanyView extends StatelessWidget {
  const CompanyView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("Company 公司");
  }
}
